import React from "react";
import { Card } from "antd";
import { Handle, Position } from "reactflow";

const boxStyle = {
  border: "2px solid #ccc",
  borderRadius: "8px",
  padding: "10px 16px",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  backgroundColor: "#fff",
  fontWeight: "bold",
};

const circleStyle = {
  ...boxStyle,
  borderRadius: "50%",
  width: 120,
  height: 120,
};

const OrchestratorStatusCard = () => {
  return (
    <Card title="OrchestratorStatusCard" style={{ overflowX: "auto" }}>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(5, 1fr)", gridTemplateRows: "repeat(5, 80px)", gap: 10 }}>

        {/* Row 2: Start → Topic Request A → Orchestrator → Topic Request Agent → Main Agent */}
        <div style={{ gridRow: 2, gridColumn: 1, ...boxStyle }}>Start</div>
        <div style={{ gridRow: 2, gridColumn: 2, ...boxStyle }}>Topic Request A</div>
        <div style={{ gridRow: 2, gridColumn: 3, ...circleStyle }}>Orchestrator</div>
        <div style={{ gridRow: 2, gridColumn: 4, ...boxStyle }}>Topic Request Agent</div>
        <div style={{ gridRow: 2, gridColumn: 5, ...boxStyle }}>Main Agent</div>

        {/* Row 3: Main Agent → Topic Response Agent */}
        <div style={{ gridRow: 3, gridColumn: 5, ...boxStyle }}>Topic Response Agent</div>

        {/* Row 4: Topic Response Agent → Orchestrator */}
        <div style={{ gridRow: 4, gridColumn: 4, ...circleStyle }}>Orchestrator</div>

        {/* Row 5: Orchestrator → Topic Response A → Start */}
        <div style={{ gridRow: 5, gridColumn: 3, ...boxStyle }}>Topic Response A</div>
        <div style={{ gridRow: 5, gridColumn: 2, ...boxStyle }}>Start</div>
      </div>
    </Card>
  );
};

export default OrchestratorStatusCard;
