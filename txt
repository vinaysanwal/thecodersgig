import React from 'react';
import { Card, Button } from 'antd';
import './OrchestratorStatusCard.css';

const OrchestratorStatusCard = () => {
  return (
    <Card title="OrchestratorStatusCard" className="orchestrator-card">
      <div className="orchestrator-flow">
        {/* Top Flow */}
        <div className="top-row">
          <div className="node">Start</div>
          <div className="arrow">→</div>
          <div className="node">Topic Request A</div>
          <div className="arrow">→</div>
          <div className="node">Orchestrator</div>
          <div className="arrow">→</div>
          <div className="node">Topic Request Agent</div>
          <div className="arrow">→</div>
          <div className="node main-agent">Main Agent</div>
        </div>

        {/* Connector from Main Agent to Topic Response Agent */}
        <div className="curve-down">↘</div>

        {/* Bottom Flow */}
        <div className="bottom-row">
          <div className="node topic-response-agent">Topic Response Agent</div>
          <div className="arrow">→</div>
          <div className="node">Orchestrator</div>
          <div className="arrow">→</div>
          <div className="node">Topic Response A</div>
          <div className="arrow">→</div>
          <div className="node">Start</div>
        </div>
      </div>
    </Card>
  );
};

export default OrchestratorStatusCard;
📁 components/OrchestratorStatusCard.css
css
Copy
Edit
.orchestrator-card {
  margin: 16px;
}

.orchestrator-flow {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.top-row, .bottom-row {
  display: flex;
  align-items: center;
  margin: 8px 0;
}

.node {
  padding: 8px 16px;
  border: 1px solid #ccc;
  border-radius: 6px;
  background-color: #f9f9f9;
  min-width: 120px;
  text-align: center;
  font-weight: 500;
}

.main-agent {
  background-color: #e6f7ff;
  border-color: #91d5ff;
  border-radius: 50%;
}

.topic-response-agent {
  background-color: #e6f7ff;
  border-color: #91d5ff;
  border-radius: 50%;
}

.arrow {
  margin: 0 6px;
  font-size: 18px;
}

.curve-down {
  margin: 8px 0;
  font-size: 24px;
  align-self: flex-end;
  margin-right: 160px;
}
