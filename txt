import React from "react";
import {
  ReactFlow,
  Background,
  Controls,
  useNodesState,
  useEdgesState,
  MarkerType,
} from "reactflow";
import { useNavigate } from "react-router-dom";
import "reactflow/dist/style.css";
import "../styles/ease.css";

const initialNodes = [
  {
    id: "datamesh",
    position: { x: 150, y: 100 },
    data: { label: "Data Mesh" },
    className: "cyber-node flow-node",
  },
  {
    id: "msteams",
    position: { x: 900, y: 100 },
    data: { label: "MS Teams" },
    className: "cyber-node flow-node",
  },
  {
    id: "observability",
    position: { x: 150, y: 550 },
    data: { label: "Observability" },
    className: "cyber-node flow-node",
  },
  {
    id: "gfagent",
    position: { x: 900, y: 550 },
    data: { label: "GF Agent" },
    className: "cyber-node flow-node clickable-node",
  },
  {
    id: "compliance",
    position: { x: 525, y: 720 },
    data: { label: "Compliance" },
    className: "cyber-node flow-node",
  },
  {
    id: "ease",
    position: { x: 525, y: 350 },
    data: {
      label: (
        <div className="ai-core-enhanced">
          <div className="core-ripple"></div>
          🤖
          <div>EASE</div>
        </div>
      ),
    },
    className: "ai-node-enhanced",
  },
];

const initialEdges = [
  "datamesh",
  "msteams",
  "observability",
  "gfagent",
  "compliance",
].map((source, index) => ({
  id: `e${index}`,
  source,
  target: "ease",
  type: "smoothstep",
  animated: true,
  markerEnd: { type: MarkerType.ArrowClosed },
}));

export default function EaseFlow() {
  const navigate = useNavigate();
  const [nodes, , onNodesChange] = useNodesState(initialNodes);
  const [edges, , onEdgesChange] = useEdgesState(initialEdges);

  const handleNodeClick = (_, node) => {
    if (node.id === "gfagent") {
      navigate("/agent/gf-agent");
    }
  };

  return (
    <div className="cyber-wrapper enhanced-bg">
      <ReactFlow
        nodes={nodes}
        edges={edges}
        onNodeClick={handleNodeClick}
        onNodesChange={onNodesChange}
        onEdgesChange={onEdgesChange}
        fitView
      >
        <Background gap={25} size={1} color="rgba(0,255,255,0.1)" />
        <Controls />
      </ReactFlow>
    </div>
  );
}
