export default function Landing() {
  return (
    <div className="landing-wrapper">

      {/* NEW HEADER */}
      <div className="top-header">
        <div className="header-left">UBS</div>
        <div className="header-divider">|</div>
        <div className="header-right">EASE</div>
      </div>

      {/* EXISTING CONTENT BELOW */}
      <div className="landing-content">
        {/* Your existing EASE glow title */}
        <h1 className="ease-header">EASE</h1>

        {/* Your existing agent grid */}
        <div className="agent-grid">
          {/* DO NOT change your card JSX */}
        </div>
      </div>

    </div>
  );
}



/* ===================================== */
/* HEADER (NEW) */
/* ===================================== */

.top-header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 65px;
  display: flex;
  align-items: center;
  padding: 0 40px;
  background: rgba(10, 15, 25, 0.6);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(0, 255, 255, 0.1);
  z-index: 100;
}

.header-left {
  font-size: 20px;
  font-weight: 700;
  color: #ff4d4f; /* UBS red */
}

.header-divider {
  margin: 0 14px;
  opacity: 0.4;
  color: #ffffff;
}

.header-right {
  font-size: 18px;
  font-weight: 600;
  color: #00ffff;
  letter-spacing: 2px;
}

/* Push content below fixed header */
.landing-content {
  padding-top: 90px;
}



.header-right {
  color: #00ffff;
  text-shadow: 0 0 10px rgba(0,255,255,0.6);
}
