const OrchestratorStatusCard = () => {
  return (
    <Card title="Orchestrator Flow" className="orchestrator-card">
      <div className="diagram-container">
        {/* Left side */}
        <div className="column">
          <div className="box">Start</div>
          <div className="arrow down">↓</div>
          <div className="box">Topic Request A</div>
          <div className="arrow right">→</div>
        </div>

        {/* Center Orchestrator */}
        <div className="center">
          <div className="ellipse">Orchestrator</div>
          <div className="center-links">
            <div className="arrow right small">→</div>
            <div className="arrow left small">←</div>
          </div>
        </div>

        {/* Right side */}
        <div className="column">
          <div className="box round">Topic Request Agent</div>
          <div className="arrow down">↓</div>
          <div className="box">Main Agent</div>
          <div className="arrow down">↓</div>
          <div className="box round">Topic Response Agent</div>
        </div>

        {/* Lower center to Orchestrator again */}
        <div className="center lower">
          <div className="arrow left">←</div>
          <div className="ellipse">Orchestrator</div>
        </div>

        {/* Left side return path */}
        <div className="column">
          <div className="arrow left">←</div>
          <div className="box">Topic Response A</div>
          <div className="arrow up">↑</div>
          <div className="box">Start</div>
        </div>
      </div>
    </Card>
  );
};

export default OrchestratorStatusCard;
🎨 components/OrchestratorStatusCard.css
css
Copy
Edit
.orchestrator-card {
  margin: 16px;
  background-color: #f8f9fa;
}

.diagram-container {
  display: grid;
  grid-template-columns: repeat(5, auto);
  grid-gap: 16px;
  justify-content: center;
  align-items: center;
  text-align: center;
  padding: 20px;
}

.column {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.center {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.center.lower {
  grid-column: span 5;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 8px;
  margin-top: 20px;
}

.box {
  border: 2px solid #ccc;
  padding: 8px 16px;
  border-radius: 6px;
  background-color: #fff;
  min-width: 140px;
}

.round {
  border-radius: 30px;
  background-color: #e6f7ff;
}

.ellipse {
  border: 2px solid #91d5ff;
  background-color: #d6f5ff;
  padding: 8px 20px;
  border-radius: 999px;
  min-width: 140px;
  text-align: center;
}

.arrow {
  font-size: 20px;
  margin: 4px 0;
}

.arrow.right {
  transform: rotate(0deg);
}
.arrow.left {
  transform: rotate(180deg);
}
.arrow.down {
  transform: rotate(90deg);
}
.arrow.up {
  transform: rotate(-90deg);
}
.arrow.small {
  font-size: 14px;
  margin: 2px 0;
}
