import React from "react";
import "./Home.css";

const agents = [
  { name: "Compliance Agent", status: "running", icon: "🛡" },
  { name: "Reminder Agent", status: "working", icon: "🔔" },
  { name: "Insight Agent", status: "idle", icon: "💡" },
  { name: "Automation Agent", status: "failed", icon: "⚙" },
  { name: "Custom Agent", status: "idle", icon: "🧩" }
];

export default function Home() {
  return (
    <div className="ease-wrapper">

      {/* HEADER */}
      <div className="ease-header">
        <h1>EASE</h1>
      </div>

      <div className="ease-main">

        {/* LEFT PANEL */}
        <div className="ease-left glass">
          <h3>WHY EASE?</h3>
          <ul>
            <li>Human & Bot Flow</li>
            <li>Unified Automation</li>
            <li>Central Orchestrator</li>
            <li>Full Observability</li>
            <li>Scalable & Secure</li>
          </ul>

          <h3 className="mt">WHAT IS EASE?</h3>
          <ul>
            <li>AI-Powered Orchestrator</li>
            <li>Agent Network</li>
            <li>Observability Built-In</li>
            <li>MCP Integrated</li>
          </ul>
        </div>

        {/* CENTER PANEL */}
        <div className="ease-center">

          <div className="ease-core glass">

            <div className="orchestrator-ring">
              <div className="orchestrator-core">
                🤖
              </div>
              <span>Orchestrator</span>
            </div>

            <div className="registry-box">Agent Registry</div>

            <div className="observability-box">
              <h4>Observability</h4>
              <p>Logs • Traces • Monitoring</p>
            </div>

          </div>

          {/* SVG Connection Lines */}
          <svg className="connection-layer">
            <defs>
              <linearGradient id="lineGradient" x1="0%" y1="0%" x2="100%" y2="0%">
                <stop offset="0%" stopColor="#00f5ff"/>
                <stop offset="100%" stopColor="#00ff99"/>
              </linearGradient>
            </defs>

            {agents.map((_, index) => (
              <path
                key={index}
                d={`M 50 100 C 250 ${150 + index * 80}, 400 ${150 + index * 80}, 600 ${150 + index * 80}`}
                stroke="url(#lineGradient)"
                strokeWidth="2"
                fill="none"
                className="flow-line"
              />
            ))}
          </svg>

          {/* MCP */}
          <div className="mcp-box">MCP</div>

          {/* DATA LAYER */}
          <div className="data-layer glass">
            <div>Data Mesh</div>
            <div>DB</div>
          </div>

        </div>

        {/* RIGHT PANEL */}
        <div className="ease-right">
          {agents.map((agent, index) => (
            <div key={index} className={`agent-module ${agent.status}`}>
              <div className="agent-circle">
                {agent.icon}
              </div>
              <div className="agent-label">
                <h4>{agent.name}</h4>
                <span>{agent.status.toUpperCase()}</span>
              </div>
            </div>
          ))}
        </div>

      </div>
    </div>
  );
}
